# Environment Configuration
# Copy this file to .env and fill in your actual values

# Server Configuration
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000

# API Authentication (Optional)
# If set, all API endpoints will require Bearer token authentication
# API_TOKEN=your_secret_api_token_here

# Proxy Configuration
# Set PROXY_TYPE to "webshare", "brightdata", or leave empty for direct connection
# PROXY_TYPE=webshare

# Webshare Proxy Configuration (if PROXY_TYPE=webshare)
# Sign up at https://webshare.io/ and get your credentials
# WEBSHARE_USERNAME=your_webshare_username
# WEBSHARE_PASSWORD=your_webshare_password

# BrightData Proxy Configuration (if PROXY_TYPE=brightdata)
# Sign up at https://brightdata.com/ and get your credentials
# Automatic IP rotation is enabled when using BrightData
# BRIGHTDATA_USERNAME=your_brightdata_username
# BRIGHTDATA_PASSWORD=your_brightdata_password
# BRIGHTDATA_ENDPOINT=brd.superproxy.io:22225